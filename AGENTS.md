# Agent instructions

The single source of truth for this repository is **`PROJECT_MASTER_CONTEXT.md`**.

Read it first. It has everything: the active product (Streamlit app), the
project map, the working scope, the branch and deploy rules, the Streamlit
pages rule, and the index of every other document.

Do not duplicate here what already lives in `PROJECT_MASTER_CONTEXT.md`. If
something changes, update it there (single source) so nothing drifts out of
sync.
